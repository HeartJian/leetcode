git rebase 
把当前分支的提交放在主分支最后一次提交之后,会以之前为基点每次都进行合并冲突
通过git rebase -i 将本地的多次提交合并为一个，以简化提交历史。本地有多个提交时,如果不进行这一步,在git rebase master时会多次解决冲突(最坏情况下,每一个提交都会相应解决一个冲突)
git cherry-pick b124e618abb420ce8fc62669705bd757d8a63f6a
将某几次提交存入